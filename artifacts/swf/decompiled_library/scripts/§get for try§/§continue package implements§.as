package §get for try§
{
   import flash.events.Event;
   
   public class §continue package implements§ extends Event
   {
      
      public static const §native var extends§:String = "CheckBoxStateChangeEvent";
      
      public function §continue package implements§(param1:String)
      {
         super(param1);
      }
   }
}

